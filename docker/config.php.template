<?php
// Database - injected from environment
$mysql_server = '${DB_HOST}';
$mysql_username = '${DB_USER}';
$mysql_password = '${DB_PASS}';
$mysql_db = '${DB_NAME}';

// Core settings
$baseurl = '${RS_BASE_URL}';
$scramble_key = '${RS_SCRAMBLE_KEY}';

// Storage
$storagedir = '/var/www/html/filestore';

// External tool paths (installed in container)
$imagemagick_path = '/usr/bin';
$ghostscript_path = '/usr/bin';
$ffmpeg_path = '/usr/bin';
$exiftool_path = '/usr/bin';

// Features
$enable_api = true;
$email_from = '${RS_EMAIL_FROM}';
$email_notify = '${RS_EMAIL_NOTIFY}';

// OpenAI GPT Integration (for automated metadata tagging)
// Set OPENAI_API_KEY environment variable or configure via Admin > System > Plugins
$openai_gpt_api_key = '${OPENAI_API_KEY}';
$openai_gpt_model = 'gpt-4o';
$openai_gpt_fallback_model = 'gpt-3.5-turbo';

// AI Faces (InsightFace) Service
// Internal Render service address - only accessible from within Render network
$faces_service_endpoint = 'http://faces-d1tp:8001';
$faces_confidence_threshold = 0.5;  // Minimum detection confidence (0-1)
$faces_match_threshold = 0.3;       // Similarity threshold for face search
$faces_tag_threshold = 0.4;         // Threshold for auto-tagging faces
